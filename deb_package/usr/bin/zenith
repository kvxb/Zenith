#!/bin/bash
# 1. Change the CWD to the application's installed folder (where data/ and lib/ are)
cd "/usr/share/zenith/linux"

# 2. Execute the runner from this correct directory
exec ./zenith_music_player "$@"